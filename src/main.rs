use machinetree_macro;

fn main() {

    // let mut controls = vec![[Control {
    //     param: 3u8,
    //     data: RefCell::new(Box::new(SampleCounter { count: 10 })),
    //     mutation_queue: RefCell::new(VecDeque::new()),
    // }]];

    // loop {
    //     controls.iter_mut().for_each(|[control]| {
    //         control.step();
    //     });
    // }
}
